import { SearchBar } from "@/browser/components/templates";

const Layout = () => {
  return (
    <div className="container py-5 flex flex-col gap-6">
      <SearchBar />
    </div>
  );
};

export default Layout;
