import { FC } from "react";
import { ProxiesList } from "@/renderer/components/templates";

const Main: FC = () => {
  return (
    <main>
      <ProxiesList />
    </main>
  );
};

export default Main;
